// in your 2D-solver page
import { useSearchParams, useRouter } from "next/navigation";

const searchParams = useSearchParams();
const router = useRouter();
const activeTab = searchParams.get("tab") || "2d";

// tab buttons become:
<button onClick={() => router.push("/2D-solver?tab=2d")}>2D Resultant</button>
<button onClick={() => router.push("/2D-solver?tab=3d")}>3D Resultant</button>